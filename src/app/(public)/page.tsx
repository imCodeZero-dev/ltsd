import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "LTSD — Limited Time Super Deals",
};

export default function LandingPage() {
  return <div>Landing Page — TODO</div>;
}
